//while do while vs for es para contadores cuando incremente o decrementa
#include <stdio.h>
int main(){
	
	int ciclo, letreros=0, num=0;
	printf("Elija un tipo de ciclo (while, do-while, for): "); //numero para cada tipo de ciclo
	scanf("%d", &ciclo);

	printf("Numero de letreros: ");
	scanf ("%d", &letreros);
	
	switch () {
	case:   ;

	
	default: 
	}
		
	return 0;
}
